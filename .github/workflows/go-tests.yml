name: macdeploy-tests

on:
  push: 
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build and test Go
        run: |
          cd src/tests
          go build ./...
          go test ./...
